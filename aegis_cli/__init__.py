"""Aegis CLI — bundled with the library.

The CLI is intentionally minimal. ``aegis check ./path`` runs the
pipeline; flags control output format and skip behavior. See
``__main__.py`` for the argparse layer.
"""
